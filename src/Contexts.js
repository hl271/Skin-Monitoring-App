import React from "react";

export const FirebaseContext = React.createContext(null);
export const AuthContext = React.createContext(null);

export const PatientContext = React.createContext(null);
export const RecordContext = React.createContext(null);
export const PatientAppointmentContext = React.createContext(null);
export const DoctorListContext = React.createContext(null);

export const DoctorContext = React.createContext(null);
export const DoctorAppointmentContext = React.createContext(null);


